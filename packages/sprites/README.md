# Common sprites for mapka

## Package the Fonts

Install required packages:

```sh
yarn install
or 
npm install
```

Generate sprites:

```sh
yarn generate
or
npm run generate
```

The sprite will created be in the `dist` directory.

## Sprites License
